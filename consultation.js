(function(){const form=document.getElementById('consultation-form');if(!form)return;form.addEventListener('submit',function(e){e.preventDefault();if(!form.reportValidity())return;const d=new FormData(form);const subject=`Consultation enquiry — ${d.get('topic')}`;const body=[`Name: ${d.get('name')}`,`Phone: ${d.get('phone')}`,`Email: ${d.get('email')||'Not provided'}`,`Preferred contact: ${d.get('contact')}`,`Practice area: ${d.get('topic')}`,`Urgency: ${d.get('urgency')}`,`Consultation type: ${d.get('consultation')}`,``,`Matter description:`,d.get('message'),``,`Consent: User acknowledged that this is a general enquiry and does not itself create an attorney-client relationship.`].join('\n');const href=`mailto:lungelomokoena8@yahoo.com?subject=${encodeURIComponent(subject)}&body=${encodeURIComponent(body)}`;const status=document.getElementById('form-status');status.textContent='Your email app should now open with the enquiry prepared. Review the details and send it to the firm.';status.classList.add('form-status-success');window.location.href=href;});})();
